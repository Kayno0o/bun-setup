# bun-setup

This npm script replaces `bun init -y` :
```bash
bunx Kayno0o/bun-setup
```

It installs `eslint`, `@kaynooo/eslint`, `@kaynooo/utils` and configure formatOnSave with eslint for vscode.
Then runs `eslint --fix`.
